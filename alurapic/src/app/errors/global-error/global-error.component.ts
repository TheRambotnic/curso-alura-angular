import { Component } from "@angular/core";

@Component({
	// não tem selector pois possui escopo de página (não será usado dentro de outro template)
	templateUrl: "./global-error.component.html"
})
export class GlobalErrorComponent {
	
}
